# BC站點包網 · Figma 操作规范（喂给 AI 用）

> 用法：贴进 claude.ai Project 的自定义指令，或每次对话开头发一遍。
> 数值全部实测自 `介面原型图` (H6RhZ96lCfh5OBwsic3Sde) 的 `个人中心-充值` 画布。

## 0. 铁律

1. **绝不修改原稿。** 任何改动画在新 frame 里，命名 `<原名> v2`，放在原 frame 右侧 +450px。
2. **每次只动一个 frame。** 改完停下来让我确认，不要连续批量改多个页面。
3. **不确定的数值不要猜。** 先读原节点，读不到就问我。
4. **不要用 Auto Layout 重排已有的绝对定位内容**，这个文件通篇 `layoutMode: none`，混用会导致元素叠在一起。

## 1. 画布与栅格

| 项 | 值 |
|---|---|
| 画板 | 390 × 844（说明页 390 × 1060） |
| 左右边距 | 24 |
| 内容宽 | 342 |
| 顶部导航高 | 76（返回按钮 44×44，位于 24,24） |
| 底部导航高 | 78（浮动条 358×62，位于 16,8；nav item 65×52） |

## 2. 间距（这是「都跌在一起」的解药）

**纵向节奏，只用这几个值：**

- 区块标题 → 该区块首个元素：**16**
- 同区块内元素之间：**10**
- 区块与区块之间：**32**
- 卡片内部 padding：**14 上下 / 14 左右**

**横向：**

- 两列卡片：单卡 164 宽，列间距 **10**（x = 24 和 198）
- 金额档位：单个 74×28，间距 **8**（x = 24 / 106 / 188 / 270）

**放置任何新元素前，先算它的 y = 上一个元素的 y + 上一个元素的 height + 上表间距。不要目测。**

## 3. 尺寸规格

| 元素 | 尺寸 | 圆角 |
|---|---|---|
| 余额卡 | 342 × 76 | 16 |
| 充值方式卡 | 164 × 48 | 12 |
| 优惠卡 | 164 × 74 | 12 |
| 金额档位 | 74 × 28 | 14（全圆） |
| 金额输入框 | 340 × 40 | 12 |
| Tab 药丸 | 48 × 30 | 15 |
| 主按钮 | 342 × 44 | 22 |
| 加码小标签 | 74 × 14 | 3 |

## 4. 颜色（只用这些，不要自创）

```
主色        #1478F5
文字/主     #121212
文字/次     #757575
文字/弱     #B3B3B3
描边/常规   #D6D6D6
描边/深     #C7C7C7
底/白       #FFFFFF
底/浅灰     #F5F5F5   （也用 #F6F6F6，二者等价，新建一律用 #F5F5F5）
标签底      #F0F2F7    标签字 #525966
深描边      #404040   （仅主按钮）
```

选中态 = 描边换 `#1478F5` + 底色换 `#EAF2FE`。不要只靠灰阶深浅区分选中。

## 5. 字体

字族一律 **Inter**。字号只用：**24 / 16 / 13 / 12 / 10 / 9**。

| 用途 | 字号 | 字重 |
|---|---|---|
| 余额数字 | 24 | Bold 700 |
| 区块标题 | 16 | Bold 700 |
| 卡片标题 | 13 | Bold 700 |
| 正文 / 输入 | 12 | Regular 400 |
| 卡片副文 | 10 | Regular 400 |
| 小标签 | 9 | Regular 400 |

金额数字一律右对齐或使用等宽数字，不要左对齐参差。

## 6. 命名

沿用现有的斜线命名：`模块 / 类型 / 状态`

```
充值方式 / 银行卡
优惠卡 / 首存×300%
Tab / 充值 Active
Icon / Alert circle / MOMO
底部导航 / Fixed
```

新建节点必须命名，禁止留 `Rectangle 3` / `Frame 12` 这种默认名。

## 7. 这个文件已知的坑（改动时顺手修，别复制）

1. **`顶部导航 #2236:279` 套了三层重复副本**，最内层残留「记录详情」标题（`#2236:284`、`#2236:290`）。新建页面时只保留一层。
2. **`底部导航 #2236:372` 里嵌了一个完整的自身副本 `#2236:389`**，两套 5 个 tab 叠在一起。
3. **`#2236:405` 文案错**：在 `Nav Item / 我的` 里却写着「搜寻」。正确的 5 项是：主页 / 俱乐部 / 充值 / 优惠 / 我的。
4. **6 个「充值说明」页是复制关系**（`#2236:277` 银行卡 / `410` MOMO / `543` Zalo / `676` Viettel / `809` 刮刮卡 / `942` USDT），结构完全一致只有选中态不同。改文案要改 6 遍。**应收敛成 Component + 6 Variant。**
5. **金额档位与限额冲突**：档位是 1000/5000/10000/15000/20000/30000，但银行卡等最低 10,000（1000、5000 不可用）、刮刮卡最低 20,000、**USDT 最低 300,000（6 个档位全部不可用）**。档位必须随所选方式动态过滤或改值。
6. **本文件本地组件数为 0**，所有 instance 来自外部资源库。要复用 `Alert circle` 用 `importComponentByKeyAsync`，key = `a84feacaafa8b5161d691ef299762a26c1dcd4eb`。
7. 文件内有 149 张图片素材，但充值相关页只用到 1 张（iPhone 16 设备外壳）。**六种支付方式目前没有 logo，全是纯文字。**

## 8. 各支付方式限额（改动时不要写错）

| 方式 | 最低 | 最高 |
|---|---|---|
| 银行卡 | 10,000 | 500,000,000 |
| MOMO | 10,000 | 15,000,000 |
| ZALO | 10,000 | 10,000,000 |
| VIETTEL | 10,000 | 50,000,000 |
| 刮刮卡 | 20,000 | 1,000,000 |
| USDT | 300,000 | 100,000,000 |

优惠：首存 ×300%（最低充值 50,000，上限 2,000,000）／存款优惠 ×10%（最低充值 50,000，上限 15,000,000）。
